image:
  pullSecret: ghcr-pull-secret

auth:
  authSecret: safezone-auth-secrets

env:
  environment: "staging"

job:
  name: "safezone-ops-smoke"
  # Default flows (no --flow args): szcli data-path smoke via cli-relay.
  command: [python, /app/smoke_test.py]

image:
  pullSecret: ghcr-pull-secret

auth:
  authSecret: safezone-auth-secrets

env:
  environment: "staging"

job:
  name: "safezone-ops-smoke"
  # No command override: the image ENTRYPOINT (python smoke_test.py)
  # runs the default szcli data-path smoke flows via cli-relay.
